// --------------------------------------------------------------------------------
// Testing functionality of <x> component.
// --------------------------------------------------------------------------------
// import Example from '../src/component/Example';

describe('Testing component', () => {

  test('it can be recognised', () => {
    // expect(Example).toBeDefined();
  });

});

describe('Testing each static method', () => {

  test('getElements', () => {
    // expect(Example.getElements()).toBeDefined();
    // expect(typeof Example.getElements()).toEqual('object');
  });

  test('getValues', () => {
    // expect(Example.getValues()).toBeDefined();
    // expect(typeof Example.getValues()).toEqual('object');
  });

  test('init', () => {
    // expect(Example.init()).toBeUndefined();
  });

});
